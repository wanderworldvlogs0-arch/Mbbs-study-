services:
  - type: web
    name: medmaster-ai
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: medmaster-ai-db
          property: connectionString
      - key: BREVO_API_KEY
        sync: false
      - key: BREVO_FROM_EMAIL
        sync: false
      - key: FRONTEND_URL
        sync: false

databases:
  - name: medmaster-ai-db
    plan: free
